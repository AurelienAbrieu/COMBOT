version: "3.8"

services:
  combot:
    build:
      context: ../..
      dockerfile: infra/docker/Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - ../../.env
    environment:
      - APP_ENV=production
      - RUNNING_IN_DOCKER=true
    restart: unless-stopped
